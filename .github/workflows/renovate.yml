name: Add changeset to Renovate updates

on:
  pull_request_target:
    types: [opened, synchronize]

jobs:
  run:
    uses: portabletext/.github/.github/workflows/renovate.yml@main
    with:
      user-login: ${{ github.event.pull_request.user.login }}
    secrets: inherit
